# 在线出「已签名」APK 详细教程（小白图文版）

> **目标**：不用在自己电脑上折腾签名工具，让 GitHub 云端自动帮你打出**带你自己签名**的 release APK（可上架应用商店、可正式发给别人安装）。
>
> **适用前提**：你已经把项目推到了 GitHub，并且用了仓库里内置的 `.github/workflows/build-apk.yml`（本教程对应的版本已支持"条件签名"）。

---

## 一、先搞懂三件事（30 秒）

| 名词 | 大白话解释 |
|---|---|
| **debug 包** | 之前默认出的包，用的是 Android 临时签名，**不能上架**，换台电脑可能装不了 |
| **release 包（已签名）** | 用**你自己的 keystore** 签的名，能上架、能正式分发 —— 这就是本教程要做的 |
| **keystore** | 你的"签名身份证"文件，**丢了就没法更新已上架的 App**，务必备份 |
| **GitHub Secrets** | GitHub 仓库自带的"保险柜"，用来安全存放密钥文本，**不会公开、不会进代码** |

> 核心思路：你把 keystore 转成一段文本，存进 GitHub 保险柜；以后每次推代码，云端就用它自动签名出包。**你不用在本机装签名环境。**

---

## 二、整体流程（一张图看懂）

```
① 生成 keystore（你的签名身份证）
        │
② 转成 base64 文本（因为保险柜只能存文字，不能存文件）
        │
③ 填进 GitHub Secrets（保险柜里放 4 条）
        │
④ 推代码 → GitHub 云端自动用密钥签名
        │
⑤ 下载 app-release.apk（已签名，可上架/分发）
```

---

## 三、第一步：生成 keystore（两种方法，任选）

### 🌟 方法 A：Android Studio 图形界面（推荐小白，不用记命令）

如果你装了 Android Studio（之前出 debug 包用的就是它），这是最省心的办法：

1. 打开 Android Studio，打开本项目（`YardAccounting` 目录）。
2. 顶部菜单点 **`Build` → `Generate Signed Bundle / APK...`**
3. 选 **`APK`**，点 **Next**。
4. 在 "Key store path" 旁边点 **`Create new...`**（创建新密钥）。
5. 填写弹窗（照着填）：

   | 字段 | 填什么 | 备注 |
   |---|---|---|
   | **Key store path** | 点右侧文件夹图标，选个好找的位置（如桌面），文件名写 `release.keystore` | 记住这个位置！ |
   | **Password** | 设一个库密码（如 `abc12345`） | ⚠️ **必须牢记** |
   | **Confirm** | 再输一遍同样的密码 | |
   | **Key alias** | 填 `yardrelease` | 要和后面 Secrets 一致 |
   | **Key password** | 设一个密钥密码（可和库密码相同） | ⚠️ **必须牢记** |
   | **Validity (years)** | 填 `25`（默认即是） | 有效期越久越好 |
   | **Certificate 下面** | 姓名/组织等随意填，能填就填 | 不影响功能 |

6. 点 **OK** 回到上一步，再点 **Next**。
7. 选 **`release`** 的 Build Variants，点 **Finish**。
8. 跑完会在你第 5 步选的位置生成 **`release.keystore`** 文件 —— 这就是你的签名身份证，**复制到安全地方备份一份**。

> 第 7 步其实也会在本地打出个 release 包，但咱们不用它，目的是拿到 `release.keystore` 这个文件。

### 方法 B：命令行（没装 Android Studio 时用）

用 **Git Bash**（装 Git for Windows 自带的，开始菜单搜 "Git Bash"）跑：

```bash
keytool -genkey -v -keystore release.keystore -alias yardrelease \
        -keyalg RSA -keysize 2048 -validity 10000
```

- 会让你设密码、填一些信息（姓名等随便填，`yardrelease` 这个 alias 别改）。
- 跑完当前目录生成 `release.keystore`。

---

## 四、第二步：把 keystore 转成 base64 文本

**为什么要转？** GitHub 保险柜（Secrets）只能存**文字**，不能直接上传文件。base64 就是把文件变成一串文字。

用 **Git Bash** 在你 `release.keystore` 所在目录跑：

```bash
base64 -w0 release.keystore > release.keystore.b64
```

- 会生成 `release.keystore.b64`，用记事本打开，是一大串字母数字（**一整行，没有换行**）。
- **复制这一整段内容备用**（等下要粘进 GitHub）。

> Windows 没 `base64` 命令时，可用 Git Bash（最省事）；或用系统自带的
> `certutil -encode release.keystore release.keystore.b64`，
> 但这样生成的文件**首尾各有一行 `-----BEGIN/END-----`**，且中间有换行，
> 需要手动删掉首尾两行、并去掉所有换行，才能粘进 Secrets。所以**优先用 Git Bash**。

---

## 五、第三步：填进 GitHub Secrets（纯网页操作）

这一步全在 GitHub 网页上点，不用命令：

1. 打开你的 GitHub 仓库页面（就是推代码那个仓库）。
2. 点仓库名右边的 **`Settings`**（设置，齿轮图标）。
3. 左侧菜单找 **`Secrets and variables` → `Actions`**，点进去。
4. 点绿色的 **`New repository secret`** 按钮。
5. **逐个添加下面 4 条**（每加完一条会回到列表，再点一次新建下一条）：

   | Secret 名称（必须一字不差） | Secret 值（填什么） |
   |---|---|
   | `KEYSTORE_BASE64` | 第四步复制的 `release.keystore.b64` **全部内容**（一大串，一整段粘进去） |
   | `KEY_ALIAS` | `yardrelease` |
   | `KEY_PASSWORD` | 你生成 keystore 时设的**密钥密码** |
   | `STORE_PASSWORD` | 你生成 keystore 时设的**库密码** |

6. 加完后，列表里应能看到这 4 条（值会被遮成圆点，正常现象）。

> ⚠️ **粘 `KEYSTORE_BASE64` 时务必是完整一整段**，别漏字符、别多空格。漏了会导致云端解码失败、出不了签名包。

---

## 六、第四步：推送代码，领取签名包

1. 确保你的代码已经推到 GitHub 的 `main` 分支（或去仓库 `Actions` 页面，左边选 `Build APK`，点 **`Run workflow`** 手动触发）。
2. 进入仓库 **`Actions`** 标签，等 `Build APK` 那次运行变绿（约 3–5 分钟）。
3. 点进这次运行，右侧 **`Artifacts`** 区域会看到：
   - **`app-debug`** —— 调试包（一直都有）
   - **`app-release`** —— ✅ **已签名包**（配好 Secrets 后才出现）
4. 下载 `app-release`，解压得到 `app-release.apk`，这就是**用你自己的密钥签名、可上架/正式分发**的安装包。

> 没看到 `app-release`？先回第五步检查 4 条 Secrets 是否都填齐、尤其是 `KEYSTORE_BASE64` 是否完整。

---

## 七、常见问题（排雷）

**Q1：怎么确认签名包真是"我的签名"？**
下载后本地用 `apksigner verify --print-certs app-release.apk`（需 Android SDK）可看证书；或更直接——装到手机能正常覆盖安装旧版（同签名）即说明一致。

**Q2：忘记 keystore 密码了怎么办？**
**没法找回。** 只能重新生成一个新 keystore，但这样一来**已上架的旧版本将无法用新包更新**（签名不一致）。所以密码和 keystore 文件务必备份好。

**Q3：base64 粘进去后云端报解码错误？**
几乎都是 `KEYSTORE_BASE64` 不完整或有换行/空格。重新用 Git Bash 生成 `.b64`，整段无换行地复制再粘一次。

**Q4：换电脑了还能出签名包吗？**
能。密钥存在 GitHub Secrets 里，和你的电脑无关。你只需在新电脑推代码即可；`release.keystore` 本体只在你需要本地签名时才需要，云端不需要原文件（用 base64 文本即可）。

**Q5：别人能看见我的密钥吗？**
不能。Secrets 在 GitHub 上始终显示为圆点，日志里也会被自动遮罩，不会泄露。

---

## 八、安全红线（必读，别踩）

- 🔐 **备份 `release.keystore`**：存到云盘/U 盘各一份。它是你 App 的"身份证"，丢了 = 无法更新已上架的 App。
- 🚫 **绝不提交进仓库**：本项目 `.gitignore` 已忽略 `keystore/` 与 `local.properties`，别手滑 `git add` 进去。
- 🔒 **Secrets 不外泄**：别把 base64 文本发给别人或贴到公开地方。
- ♻️ **一套密钥用到底**：同一个 App 永远用同一个 keystore，别每次重新生成。

---

> 更简洁的版本也在 `README.md` 的「🔐 在线出已签名 APK」一节。本教程是给不熟命令的同学准备的详细图文版。
